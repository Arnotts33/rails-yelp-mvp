<h1>Welcome to our guide!</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
    <%= link_to restaurant.name, restaurant_path(restaurant) %> | <%= restaurant.category %> | <%= restaurant.address %>
    </li>
  <% end %>

  <%= link_to 'Add a new restaurant', new_restaurant_path(@restaurant) %>

</ul>
